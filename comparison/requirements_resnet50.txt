# Minimal dependencies to TRAIN/TEST the ResNet-50 attributor of
# ImageAttributionBench. Heavy/irrelevant deps from environment.yaml
# (flash-attn, torchaudio+rocm, openai-CLIP, wand, pyaudio, multicoretsne,
# tensorflow-datasets, diffusers, ...) are NOT needed for resnet50.
#
# Install torch/torchvision from the CUDA 12.1 index (Leonardo A100):
#   pip install torch==2.3.0 torchvision==0.18.0 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r comparison/requirements_resnet50.txt
#
# (If you reuse an existing env that already has a working CUDA torch,
#  just `pip install` the lines below the torch/torchvision pins.)

torch==2.3.0
torchvision==0.18.0

numpy
pillow
scikit-learn
scipy
pyyaml
pandas
tqdm
opencv-python
albumentations==1.3.1
timm
yacs
tensorboard
matplotlib
seaborn
